// if the majority of airports in a country have the same prefix, color the country with that prefix
// and color the other airports individually
// if the majority of airports do not meet this criteria, only colors airports, but not the country
export const qualifiedMajority = 0.9;
